{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsx ~/.claude/hooks/ts/block-dangerous-commands.ts"
          }
        ]
      }
    ]
  },
  "_comment": "This configuration adds dangerous command blocking to Claude Code",
  "_installation": [
    "1. Copy the entire hooks/ts directory to ~/.claude/hooks/ts/",
    "2. This includes the config/dangerous-commands.json file",
    "3. Copy this file to ~/.claude/settings.json",
    "4. Customize ~/.claude/hooks/ts/config/dangerous-commands.json with your own rules"
  ],
  "_combining_with_other_hooks": {
    "note": "You can combine this with other hooks like auto-approve",
    "example": "Add multiple command entries in the hooks array",
    "order": "Dangerous command blocking should run before auto-approve"
  }
}