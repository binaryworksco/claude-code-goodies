# Claude Code Goodies - Configuration
# Copy this file to ~/.claude/.env and fill in your values

# Sound Notification Settings (macOS)
# SOUND_NOTIFICATIONS_ENABLED="true"  # Set to "false" to disable all sound notifications

# Sounds for different events (choose from: Basso, Blow, Bottle, Frog, Funk, Glass, Hero, Morse, Ping, Pop, Purr, Sosumi, Submarine, Tink)
# STOP_SOUND="Glass"                 # Sound when Claude completes (default: Glass)
# NOTIFICATION_SOUND="Ping"          # Sound when Claude needs input (default: Ping)
# PRETOOLUSE_BLOCK_SOUND="Basso"     # Sound when a command is blocked (default: Basso)
# ERROR_SOUND="Sosumi"               # Sound when an error occurs (default: Sosumi)

# Legacy option (will be used for STOP_SOUND if STOP_SOUND is not set)
# COMPLETION_SOUND="Glass"

# Logging Settings
# COMMAND_FILTER_LOG_ENABLED="true"     # Set to "false" to disable command filter logging (default: true)
# HOOK_ACTIVITY_LOG_ENABLED="true"      # Set to "false" to disable hook activity logging (default: true)